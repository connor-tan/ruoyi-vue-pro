<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.promotion.dal.mysql.bargain.BargainRecordMapper">

    <select id="selectUserCountByStatus" resultType="java.lang.Integer">
        SELECT COUNT(DISTINCT(user_id))
        FROM promotion_bargain_record
        WHERE status = #{status}
    </select>

    <select id="selectUserCountByActivityIdAndStatus" resultType="java.lang.Integer">
        SELECT COUNT(DISTINCT(user_id))
        FROM promotion_bargain_record
        WHERE activity_id = #{activityId}
          AND status = #{status}
    </select>

</mapper>
