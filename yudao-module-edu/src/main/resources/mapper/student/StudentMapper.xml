<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.edu.dal.mysql.student.StudentMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="selectDistinctCurrentSchoolIds" resultType="java.lang.Long">
        SELECT DISTINCT current_school_id
        FROM edu_student
        WHERE deleted = b'0'
          AND current_school_id IS NOT NULL
        ORDER BY current_school_id ASC
    </select>

    <select id="selectDistinctCurrentSchoolIdsByStatuses" resultType="java.lang.Long">
        SELECT DISTINCT current_school_id
        FROM edu_student
        WHERE deleted = b'0'
          AND current_school_id IS NOT NULL
          AND status IN
        <foreach collection="statuses" item="status" open="(" separator="," close=")">
            #{status}
        </foreach>
        ORDER BY current_school_id ASC
    </select>

    <update id="updateStatusById">
        UPDATE edu_student
        SET status = #{status},
            update_time = NOW()
        WHERE id = #{id}
          AND deleted = b'0'
    </update>

</mapper>
